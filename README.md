# Air-Pollution-Predictor
Using the dataset from the UCI repository, we have designed a RNN model to predict the air polution index for the next hour.
This model uses a LSTM designed using the keras library for python.
